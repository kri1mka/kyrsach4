<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kyrsach4"
        tools:targetApi="31"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MyProfileActivityKs"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".HelpActivity" />
        <activity android:name=".PdfActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ChangeDataActivityKs"/>
        <activity android:name=".CreatePublicationActivityKs"/>
        <activity android:name=".CreateTripActivityKs"/>
        <activity android:name=".TripsActivity2" />
        <activity android:name=".CreatePostDetailsActivityKs" />

    </application>

</manifest>